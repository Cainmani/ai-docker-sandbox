name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly Monday 06:00 UTC — keeps the pinned-version check running
    # even when the repo is idle, so stale tool pins get flagged
    - cron: '0 6 * * 1'

# Restrict token to read-only — CI does not need write access
permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Validate PowerShell syntax
        shell: pwsh
        run: |
          $errors = @()

          Get-ChildItem -Path "scripts/*.ps1" | ForEach-Object {
            Write-Host "Checking: $($_.Name)"
            try {
              $null = [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$null, [ref]$null)
              Write-Host "  ✓ Syntax OK" -ForegroundColor Green
            } catch {
              Write-Host "  ✗ Syntax Error: $($_.Exception.Message)" -ForegroundColor Red
              $errors += $_.FullName
            }
          }

          if ($errors.Count -gt 0) {
            Write-Error "Syntax errors found in: $($errors -join ', ')"
            exit 1
          }

      - name: Validate Dockerfile
        shell: bash
        run: |
          echo "Checking Dockerfile syntax..."
          if docker build --check -f docker/Dockerfile docker/ 2>/dev/null; then
            echo "✓ Dockerfile syntax OK"
          else
            # --check not available in all Docker versions, try basic validation
            if grep -q "^FROM" docker/Dockerfile; then
              echo "✓ Dockerfile has FROM instruction"
            else
              echo "✗ Dockerfile missing FROM instruction"
              exit 1
            fi
          fi

      - name: Check for common issues
        shell: pwsh
        run: |
          $issues = @()

          # Check for hardcoded paths that might not work on other systems
          $files = Get-ChildItem -Path "scripts/*.ps1" -Recurse
          foreach ($file in $files) {
            $content = Get-Content $file.FullName -Raw
            if ($content -match 'C:\\Users\\[^$]') {
              $issues += "$($file.Name): Contains hardcoded user path"
            }
          }

          # Check for TODO/FIXME comments
          $todoCount = (Get-ChildItem -Path "scripts/*.ps1" | Select-String -Pattern "TODO|FIXME" -AllMatches).Matches.Count
          if ($todoCount -gt 0) {
            Write-Host "Note: Found $todoCount TODO/FIXME comments" -ForegroundColor Yellow
          }

          if ($issues.Count -gt 0) {
            Write-Warning "Issues found:"
            $issues | ForEach-Object { Write-Warning "  - $_" }
          }

          Write-Host "Validation complete"

      - name: Install ShellCheck
        shell: bash
        run: |
          if ! command -v shellcheck &>/dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck
          fi
          shellcheck --version

      - name: Lint shell scripts (blocking errors)
        shell: bash
        run: |
          mapfile -t scripts < <(find docker tests -type f -name '*.sh' -print | sort)
          shellcheck --severity=error "${scripts[@]}"

      - name: Lint shell scripts (visible warnings)
        shell: bash
        continue-on-error: true
        run: |
          mapfile -t scripts < <(find docker tests -type f -name '*.sh' -print | sort)
          shellcheck --severity=warning "${scripts[@]}"

      - name: Lint Dockerfile (hadolint)
        shell: bash
        continue-on-error: true
        run: |
          if ! command -v hadolint &>/dev/null; then
            sudo curl -sL "https://github.com/hadolint/hadolint/releases/download/v2.12.0/hadolint-Linux-x86_64" -o /usr/local/bin/hadolint
            sudo chmod +x /usr/local/bin/hadolint
          fi
          echo "hadolint version: $(hadolint --version)"
          echo ""
          hadolint docker/Dockerfile

      - name: Install Pester
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object Version -ge '5.0')) {
            Install-Module -Name Pester -Force -SkipPublisherCheck -MinimumVersion 5.0
          }
          Import-Module Pester
          Write-Host "Pester version: $((Get-Module Pester).Version)"

      - name: Run Pester tests
        shell: pwsh
        run: |
          $config = New-PesterConfiguration
          $config.Run.Path = 'tests'
          $config.Filter.Tag = @()
          $config.Filter.ExcludeTag = @()
          $config.TestResult.Enabled = $true
          $config.TestResult.OutputFormat = 'JUnitXml'
          $config.TestResult.OutputPath = 'tests/results.xml'
          $config.Output.Verbosity = 'Detailed'
          Invoke-Pester -Configuration $config

      - name: Run bash tests
        shell: bash
        run: |
          exit_code=0
          for test_file in tests/test_*.sh; do
            echo "=== Running $test_file ==="
            chmod +x "$test_file"
            if ! bash "$test_file"; then
              echo "FAILED: $test_file"
              exit_code=1
            fi
          done
          exit $exit_code

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pester-results
          path: tests/results.xml

  windows-pester:
    name: Pester (${{ matrix.shell_name }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - shell_name: Windows PowerShell 5.1
            shell: powershell
          - shell_name: PowerShell 7
            shell: pwsh
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      # Two steps guarded by matrix.shell rather than `shell: ${{ matrix.shell }}`:
      # GitHub does not accept the matrix context in the step `shell` key, so we
      # branch explicitly to run the same Pester invocation under each shell.
      - name: Install and run Pester (Windows PowerShell 5.1)
        if: matrix.shell == 'powershell'
        shell: powershell
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object Version -ge '5.0')) {
            Install-Module -Name Pester -Force -SkipPublisherCheck -MinimumVersion 5.0 -Scope CurrentUser
          }
          Import-Module Pester
          $result = Invoke-Pester -Path tests -PassThru
          if ($result.FailedCount -gt 0) { exit 1 }

      - name: Install and run Pester (PowerShell 7)
        if: matrix.shell == 'pwsh'
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object Version -ge '5.0')) {
            Install-Module -Name Pester -Force -SkipPublisherCheck -MinimumVersion 5.0 -Scope CurrentUser
          }
          Import-Module Pester
          $result = Invoke-Pester -Path tests -PassThru
          if ($result.FailedCount -gt 0) { exit 1 }

  version-consistency:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Verify release version sources
        shell: pwsh
        run: |
          $version = (Get-Content VERSION -Raw).Trim()
          if ($version -notmatch '^\d+\.\d+\.\d+$') { throw "VERSION is not semantic: $version" }
          foreach ($path in @('scripts/AI_Docker_Complete.ps1', 'scripts/AI_Docker_Launcher.ps1')) {
            $content = Get-Content $path -Raw
            if ($content -notmatch ('\$script:AppVersion\s*=\s*"' + [regex]::Escape($version) + '"')) {
              throw "$path AppVersion does not match VERSION $version"
            }
          }
          $changelog = Get-Content CHANGELOG.md -Raw
          if ($changelog -notmatch ('(?m)^## \[' + [regex]::Escape($version) + '\]')) {
            throw "Latest release $version is missing from CHANGELOG.md"
          }
          # The wizard must propagate the product version into docker/.env, or the
          # built image silently falls back to the compose default (0.0.0), which
          # the version-skew check would misread as a legacy image.
          $wizard = Get-Content scripts/setup_wizard.ps1 -Raw
          if ($wizard -notmatch "Set-EnvKey[^\r\n]*'AI_DOCKER_VERSION'") {
            throw "setup_wizard.ps1 does not propagate AI_DOCKER_VERSION into docker/.env"
          }

  check-pinned-versions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '22'

      - name: Check pinned package versions
        shell: bash
        run: |
          echo "Checking pinned package versions against registries..."
          echo ""
          stale=0

          # Extract pinned versions from install_cli_tools.sh
          SCRIPT="docker/install_cli_tools.sh"

          check_npm() {
            local pkg="$1"
            local pinned="$2"
            local latest
            latest=$(npm view "$pkg" version 2>/dev/null || echo "unknown")
            if [ "$latest" = "unknown" ]; then
              echo "⚠ $pkg: could not query registry (pinned: $pinned)"
            elif [ "$pinned" = "$latest" ]; then
              echo "✓ $pkg: $pinned (up to date)"
            else
              echo "⚠ $pkg: pinned $pinned → latest $latest (UPDATE AVAILABLE)"
              stale=$((stale + 1))
            fi
          }

          check_pip() {
            local pkg="$1"
            local pinned="$2"
            local latest="unknown"
            # Try pip index versions (format varies by pip version)
            local pip_output
            pip_output=$(pip3 index versions "$pkg" 2>/dev/null || true)
            if [ -n "$pip_output" ]; then
              latest=$(echo "$pip_output" | grep -iE 'LATEST|Available' | grep -oE '[0-9]+\.[0-9]+[0-9.]*' | head -1 || true)
            fi
            # Fallback: use pip install with invalid version to extract latest from error
            if [ -z "$latest" ] || [ "$latest" = "unknown" ]; then
              latest=$(pip3 install "${pkg}==0.0.0invalid" 2>&1 | grep -oE '[0-9]+\.[0-9]+[0-9.]*' | tail -1 || echo "unknown")
            fi
            if [ -z "$latest" ] || [ "$latest" = "unknown" ]; then
              echo "⚠ $pkg: could not query registry (pinned: $pinned)"
            elif [ "$pinned" = "$latest" ]; then
              echo "✓ $pkg (pip): $pinned (up to date)"
            else
              echo "⚠ $pkg (pip): pinned $pinned → latest $latest (UPDATE AVAILABLE)"
              stale=$((stale + 1))
            fi
          }

          # Parse pinned versions from the script
          gemini_ver=$(grep -oP '@google/gemini-cli@\K[0-9.]+' "$SCRIPT" | head -1)
          codex_ver=$(grep -oP '@openai/codex@\K[0-9.]+' "$SCRIPT" | head -1)
          vibe_ver=$(grep -oP 'vibe-kanban@\K[0-9.]+' "$SCRIPT" | head -1)
          ninerouter_ver=$(grep -oP '9router@\K[0-9.]+' "$SCRIPT" | head -1)
          omniroute_ver=$(grep -oP 'omniroute@\K[0-9.]+' "$SCRIPT" | head -1)
          openai_ver=$(grep -oP 'openai==\K[0-9.]+' "$SCRIPT" | head -1)

          echo "Pinned versions found in $SCRIPT:"
          echo "  @google/gemini-cli: ${gemini_ver:-NOT FOUND}"
          echo "  @openai/codex: ${codex_ver:-NOT FOUND}"
          echo "  vibe-kanban: ${vibe_ver:-NOT FOUND}"
          echo "  9router: ${ninerouter_ver:-NOT FOUND}"
          echo "  omniroute: ${omniroute_ver:-NOT FOUND}"
          echo "  openai (pip): ${openai_ver:-NOT FOUND}"
          echo ""

          # Check each against registry
          [ -n "$gemini_ver" ] && check_npm "@google/gemini-cli" "$gemini_ver"
          [ -n "$codex_ver" ] && check_npm "@openai/codex" "$codex_ver"
          [ -n "$vibe_ver" ] && check_npm "vibe-kanban" "$vibe_ver"
          [ -n "$ninerouter_ver" ] && check_npm "9router" "$ninerouter_ver"
          [ -n "$omniroute_ver" ] && check_npm "omniroute" "$omniroute_ver"
          [ -n "$openai_ver" ] && check_pip "openai" "$openai_ver"

          echo ""
          if [ "$stale" -gt 0 ]; then
            echo "::warning::$stale pinned package(s) have newer versions available. Update versions in docker/install_cli_tools.sh."
          else
            echo "All pinned packages are up to date."
          fi
